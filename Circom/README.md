## Setup:
To install all required prerequisites and copy all scripts into the created folder execute `chmod +x setup_circom.sh` followed by `./setup_circom.sh`.

## Run Benchmark:
To run the full benchmark, `cd circomlib/circuits/sha256` and start the benchmark with `./run_benchmark.sh`.