## Setup:
To install all required prerequisites and copy all scripts into the created folder execute `chmod +x setup_halo.sh` followed by `./setup_halo.sh`.

## Run Benchmark:
To run the full benchmark, `cd halo` and start the benchmark with `./run_benchmark.sh`.