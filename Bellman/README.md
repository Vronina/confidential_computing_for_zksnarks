## Setup:
To install all required prerequisites and copy all scripts into the created folder execute `chmod +x setup_bellman.sh` followed by `./setup_bellman.sh`.

## Run Benchmark:
To run the full benchmark, `cd sha_circ` and start the benchmark with `./run_benchmark.sh`.